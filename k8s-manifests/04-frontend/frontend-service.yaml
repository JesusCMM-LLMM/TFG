# Expone el puerto 80 del frontend dentro del clúster.

apiVersion: v1
kind: Service
metadata:
  name: proyecto-frontend
  namespace: proyecto-tfg-2026
spec:
  selector:
    app: proyecto-frontend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 80
  type: ClusterIP